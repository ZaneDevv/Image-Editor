function EnableProjectEditing() {
    /* Enable controls */
    EnableZoom();
    EnableMovement();
    
    /* Enable tools */
    EnableInsertText();
    EnableInsertImage();
    EnableRotationTool();
    EnableColorSelection();
}