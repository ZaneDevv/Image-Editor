function EnableProjectEditing() {
    EnableFullScreenButton();

    /* Enable controls */
    EnableZoom();
    EnableMovement();
    EnableCopyPaste();
    
    /* Enable tools */
    EnableInsertText();
    EnableInsertImage();
    EnableRotationTool();
    EnableColorSelection();
    EnableScaleTool();
}