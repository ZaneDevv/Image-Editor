function EnableProjectEditing() {
    EnableFullScreenButton();

    /* Enable controls */
    EnableZoom();
    EnableMovement();
    EnableCopyPaste();
    EnableUndoRedo();
    

    /* Enable tools */
    EnableInsertText();
    EnableInsertImage();
    EnableRotationTool();
    EnableColorSelection();
    EnableScaleTool();
}