function EnableProjectEditing() {
    /* Enable controls */
    EnableZoom();
    EnableMovement();
    
    /* Enable tools */
    EnableInsertText();
    EnableRotationTool();
    EnableColorSelection();
}